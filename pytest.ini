[pytest]
# Configuración global de Pytest para el proyecto

# Directorio donde se buscan los tests
testpaths = tests

# Opciones por defecto al correr pytest
addopts = -v --tb=short

# Formato de los IDs de los tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
